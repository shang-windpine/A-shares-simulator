[package]
name = "core_entities"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.41", features = ["serde"] }
rust_decimal = "1.37.1"
serde = { version = "1.0", features = ["derive"] } 